@model List<WebDemo.Models.CartItem>
@if (Model.Count > 0)
{
    <li><a href="@Url.Action("Index","Cart")"><i class="fa fa-shopping-cart"></i><label style="font-size: xx-large;color: #fe980f;"> @Model.Count</label> </a></li>
}
else
{
    <li><a href="@Url.Action("Index","Cart")"><i class="fa fa-shopping-cart"></i> Giỏ hàng </a></li>
}